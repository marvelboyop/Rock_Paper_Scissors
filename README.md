# 🎮 Rock, Paper, Scissors in C++

A simple console-based **Rock-Paper-Scissors** game built using C++.

## 🔹 Features
- Randomized computer choice using `rand()` and `srand(time(0))`
- Input validation with `cin.clear()` and `cin.ignore()`
- Replay feature with score tracking
- Clean, readable structure with comments

## 🧠 Concepts Used
- Loops (`do-while`)
- Conditional logic (`if-else`)
- Random number generation
- User input validation

## 🏁 How to Run
1. Compile:
   ```bash
   g++ RockPaperScissors.cpp -o game
2. Run:
   ```bash
   ./game

## Enjoy the game 🎉
